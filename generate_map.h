#ifndef GENERATE_MAP_H
#define GENERATE_MAP_H

// Initialize the map with char c
void initializeMap(char map[25][100], char c);

// Generate a random map
void generateRandomMap(char map[25][100]);

#endif